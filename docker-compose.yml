version: '3.8'
services:
  zephyr:
    build: .
    platform: linux/amd64
    volumes:
      - ./workspace:/workspace
      - ./app:/workspace/app
      - ./scripts:/workspace/scripts
    working_dir: /workspace
    stdin_open: true
    tty: true
